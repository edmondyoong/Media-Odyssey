# Build, Push, and Deploy Docker image to Render with environment variables updated with secrets
# Portion uses "Publishing images to GitHub Packages" example in GitHub Docs: https://docs.github.com/en/actions/tutorials/publish-packages/publish-docker-images
#
# Image is being built in a workflow within GitHub Actions, instead of Render, because Render is very slow at building Docker images.
name: build-push-deploy-image

# Configures this workflow to run every time a change is pushed to the branch called `main`.
on:
  push:
    branches: ["**"]

  workflow_dispatch:

# Defines two custom environment variables for the workflow. These are used for the Container registry domain, and a name for the Docker image that this workflow builds.
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

# There is a single job in this workflow. It's configured to run on the latest available version of Ubuntu.
jobs:
  build-push-deploy-image:
    runs-on: ubuntu-latest
    # environment: ${{ github.ref_name }}
    environment: misc/workflows

    # Sets the permissions granted to the `GITHUB_TOKEN` for the actions in this job.
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      # Uses the `docker/login-action` action to log in to the Container registry registry using the account and password that will publish the packages. Once published, the packages are scoped to the account defined here.
      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # This step uses [docker/metadata-action](https://github.com/docker/metadata-action#about) to extract tags and labels that will be applied to the specified image. The `id` "meta" allows the output of this step to be referenced in a subsequent step. The `images` value provides the base name for the tags and labels.
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      # This step uses the `docker/build-push-action` action to build the image, based on your repository's `Dockerfile`. If the build succeeds, it pushes the image to GitHub Packages.
      # It uses the `context` parameter to define the build's context as the set of files located in the specified path. For more information, see [Usage](https://github.com/docker/build-push-action#usage) in the README of the `docker/build-push-action` repository.
      # It uses the `tags` and `labels` parameters to tag and label the image with the output from the "meta" step.
      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@f2a1d5e99d037542a71f64918e516c093c6f3fc4
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # This step generates an artifact attestation for the image, which is an unforgeable statement about where and how it was built. It increases supply chain security for people who consume the image. For more information, see [Using artifact attestations to establish provenance for builds](/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds).
      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v3
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME}}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      # Sync Render env vars from a single JSON secret to support any number of variables.
      # This is optional; if RENDER_ENV_VARS_JSON is empty, it skips this step.
      # 
      # This step was primarily written by GitHub Copilot.
      - name: Sync Render environment variables
        env:
          RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_ENV_VARS_JSON: ${{ secrets.RENDER_ENV_VARS_JSON }}
        run: |
          if [ -z "$RENDER_ENV_VARS_JSON" ]; then
            exit 0
          fi

          echo "$RENDER_ENV_VARS_JSON" | jq -rc 'to_entries[] | @base64' | while IFS= read -r row; do
            entry=$(echo "$row" | base64 --decode)
            key=$(echo "$entry" | jq -r '.key')
            value=$(echo "$entry" | jq -r '.value')
            echo "::add-mask::$value"

            payload=$(jq -n --arg value "$value" '{value: $value}')

            curl --fail --show-error --silent -X PUT \
              "https://api.render.com/v1/services/${RENDER_SERVICE_ID}/env-vars/${key}" \
              --header 'accept: application/json' \
              --header 'content-type: application/json' \
              --header "authorization: Bearer ${RENDER_API_KEY}" \
              --data "$payload" \
              --output /dev/null
          done
      
      # This step triggers a deployment to Render with the pre-built Docker image link.
      # Debugging this workflow was assisted by GitHub Copilot.
      - name: Deploy to Render with curl
        run: |
          IMAGE_TAG="${{ fromJSON(steps.meta.outputs.json).tags[0] }}"
          IMAGE_NAME_ONLY="${IMAGE_TAG%:*}"
          RENDER_DIGEST_TAG="${{ steps.push.outputs.digest }}"
          RENDER_DIGEST_TAG="${RENDER_DIGEST_TAG/:/-}"
          IMAGE_URL="${IMAGE_NAME_ONLY}:${RENDER_DIGEST_TAG}"

          PAYLOAD=$(cat <<EOF
          {
            "clearCache": "clear",
            "imageUrl": "${IMAGE_URL}"
          }
          EOF
          )

          curl --fail --show-error --silent -X POST \
            "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys" \
            --header 'accept: application/json' \
            --header 'content-type: application/json' \
            --header 'authorization: Bearer ${{ secrets.RENDER_API_KEY }}' \
            --data "$PAYLOAD"
